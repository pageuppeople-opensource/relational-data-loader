version: 1.0.{build}
build: off



environment:
  PGUSER: "postgres"
  PGPASSWORD: "Password12!"
  PGPORT: 543211
  PGHOST: "localhost"

services:
  - postgresql101

init:
  - "set PATH=C:\\Program Files\\PostgreSQL\\10\\bin\\;%PATH%"
  - "psql -c \"SELECT VERSION()\""
